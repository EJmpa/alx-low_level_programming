#include "main.h"

/**
 * _abs - Return absolute value of an integer
 * @n: Integer to be coputed
 *
 * Return: Absolute Value of Integer
 */

int _abs(int n)

{
	if (n >= 0)
		return (n);

	else
		return (n * -1);
}
